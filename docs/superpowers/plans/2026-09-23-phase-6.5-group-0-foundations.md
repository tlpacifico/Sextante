# Phase 6.5 — Grupo 0 (Fundações: ADR + dívida técnica) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fechar a dívida técnica que a Phase 6.5 herda (RLS em falta, reaplicar regras limitado, import sem eventos, totais a perder categorias arquivadas, arquivos sem guarda) e registar o ADR-014, antes de mexer no modelo da `Transaction`.

**Architecture:** Correções locais em módulos existentes do `Financial`, sem mudanças de modelo de domínio. Uma migration EF nova só com SQL (RLS). Handlers Wolverine ganham `IIntegrationEventPublisher` onde faltava. Repositório de transações passa a juntar categorias por `(Id, TenantId)` ignorando o filtro de soft-delete.

**Tech Stack:** .NET 10, EF Core + Npgsql, Wolverine, PostgreSQL 16 RLS, xUnit + FluentAssertions + Testcontainers.

**Spec:** `specs/2026-09-23-phase-6.5-transfers-credit-card/` (`plan.md` §0, `requirements.md` §"0. Dívida técnica", `validation.md` bullets 16–20, 24).

## Global Constraints

- Mensagens de erro, comentários de domínio e commits em **PT-PT**; identifiers em inglês (`AGENTS.md` §2.5).
- Toda tabela tenant-owned: `ENABLE` + `FORCE ROW LEVEL SECURITY`, policy `<tabela>_tenant_isolation` com `USING` e `WITH CHECK` sobre `current_setting('app.current_tenant_id', false)::uuid`, CHECK sentinel `tenant_id <> 'ffffffff-ffff-ffff-ffff-ffffffffffff'` (`AGENTS.md` §3.1, padrão `20260502133245_AddBudgetsAndAlerts.cs:122-157`).
- Escritas fora de uma request HTTP (subscribers Wolverine) definem o tenant com `set_config('app.current_tenant_id', …, true)` dentro de uma transação (padrão `CategoryRepository.SeedAsync`).
- Eventos de integração publicados **depois** do `SaveChangesAsync` (padrão `TransactionHandlers.cs:66-78`).
- `dotnet format` limpo (o CI falha com formatação).
- Ficheiros em LF (commit `4beceb6`).

## Review Focus

1. **Signup depois do RLS**: `SeedDefaultImportProfilesHandler` escreve em `import_profiles` num subscriber sem tenant na ligação; com RLS ligado o signup tem de continuar a criar o perfil ActivoBank. Teste em Task 2.
2. **Migration sobre dados existentes** (VPS e local já têm linhas nas três tabelas): o `ADD CONSTRAINT … CHECK` valida linhas existentes e a policy não pode esconder linhas do próprio tenant. Teste em Task 2 (leitura pelo tenant dono continua a devolver as linhas).
3. **Reapply com filtro de categoria**: paginar por cursor enquanto se altera a categoria das linhas já lidas não pode saltar nem repetir linhas. Teste em Task 3.
4. **Categoria arquivada no donut e no export**: a transação continua a aparecer com o nome da categoria arquivada; nenhuma categoria de outro tenant entra no join ao ignorar o filtro global. Teste em Task 4.
5. **Arquivar conta sem transações** continua a funcionar (204) e com transações devolve 400 com mensagem PT-PT, sem arquivar. Teste em Task 6.

---

### Task 1: ADR-014 e tech-stack

**Files:**
- Create: `docs/adr/ADR-014-transfers-and-account-balance.md`
- Modify: `specs/tech-stack.md` (§17 tabela, §18 "Escritos no repo" e "Pendentes")

**Interfaces:** Produces: decisões D1–D6 de `requirements.md` registadas; nenhuma API.

- [ ] **Step 1: Escrever o ADR**

Estrutura igual à do `ADR-013` (Estado / Data / Autores / Context / Decision / Alternatives / Consequences). Conteúdo:
- *Context*: primeiro dogfooding com conta à ordem + cartão; direção inferida da categoria; sem saldo; pagamento do cartão contado duas vezes.
- *Decision*: transferência = par de `Transaction` com `TransferId` e `Kind = Transfer`, sem categoria; `Direction` explícita (`Inflow`/`Outflow`), backfill a partir de `Category.Kind`; `CategoryId` nullable com CHECK por `Kind`; totais/donut/orçamentos só `Kind = Regular`; saldo on-demand = `OpeningBalance + Σ Inflow − Σ Outflow` desde `OpeningBalanceDate`.
- *Alternatives*: entidade `Transfer` própria (rejeitada — duas fontes em todas as listagens e no saldo); categoria de sistema "Transferência" (rejeitada — não liga as pernas, totais continuam a depender de exclusões por nome).
- *Consequences*: todas as agregações deixam de depender de `Category.Kind`; `PublicApi` inalterado; migration com backfill.

- [ ] **Step 2: Atualizar tech-stack**

§17: acrescentar linhas `| Transferências entre contas | Par de transações ligadas (TransferId), ADR-014 |` e `| Saldo de conta | On-demand desde OpeningBalanceDate, ADR-014 |`. §18: mover ADR-014 de "Pendentes" para "Escritos no repo".

- [ ] **Step 3: Commit**

```bash
git add docs/adr/ADR-014-transfers-and-account-balance.md specs/tech-stack.md
git commit -m "Escrever ADR-014 (transferências e saldo de conta)"
```

---

### Task 2: RLS nas tabelas do import + seed do perfil com tenant

**Files:**
- Create: `src/Modules/Financial/Sextante.Modules.Financial.Infrastructure/Persistence/Migrations/<timestamp>_AddMissingRlsToImportTables.cs` (gerada com `dotnet ef migrations add`, corpo só SQL)
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Domain/ImportProfiles/IImportProfileRepository.cs` (novo `SeedAsync`)
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Infrastructure/Persistence/Repositories/ImportProfileRepository.cs`
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Application/Integration/SeedDefaultImportProfilesHandler.cs`
- Test: `tests/Sextante.IntegrationTests/MultiTenancy/ImportTablesRlsTests.cs`

**Interfaces:**
- Produces: `Task SeedAsync(Guid tenantId, ImportProfile profile, CancellationToken cancellationToken)` em `IImportProfileRepository`.

- [ ] **Step 1: Teste que falha**

```csharp
using FluentAssertions;
using Npgsql;
using Sextante.IntegrationTests.Financial;

namespace Sextante.IntegrationTests.MultiTenancy;

/// <summary>
/// Phase 6.5 §0.2 — as três tabelas do import (Phase 4) ficaram sem RLS.
/// </summary>
public sealed class ImportTablesRlsTests : IClassFixture<IdentityIntegrationFixture>
{
    private readonly IdentityIntegrationFixture _fixture;

    public ImportTablesRlsTests(IdentityIntegrationFixture fixture) => _fixture = fixture;

    [Theory]
    [InlineData("categorization_rules")]
    [InlineData("import_profiles")]
    [InlineData("import_batches")]
    public async Task Table_has_rls_enabled_and_forced(string table)
    {
        await using var conn = _fixture.OpenSuperuserConnection();
        await using var cmd = new NpgsqlCommand(
            "SELECT relrowsecurity AND relforcerowsecurity FROM pg_class c " +
            "JOIN pg_namespace n ON n.oid = c.relnamespace " +
            "WHERE n.nspname = 'financial' AND c.relname = @t", conn);
        cmd.Parameters.AddWithValue("t", table);
        ((bool)(await cmd.ExecuteScalarAsync())!).Should().BeTrue();
    }

    [Fact]
    public async Task Signup_still_seeds_default_import_profile_visible_only_to_its_tenant()
    {
        var (clientA, tenantA, _) = await FinancialTestHelpers.SignupAndLoginAsync(_fixture, "rls-imp-a");
        var (_, tenantB, _) = await FinancialTestHelpers.SignupAndLoginAsync(_fixture, "rls-imp-b");

        // Seed corre num subscriber assíncrono — esperar até aparecer.
        List<System.Text.Json.JsonElement>? profiles = null;
        for (var i = 0; i < 50 && (profiles is null || profiles.Count == 0); i++)
        {
            profiles = await clientA.GetFromJsonAsync<List<System.Text.Json.JsonElement>>(
                "/api/financial/import-profiles");
            if (profiles!.Count == 0) await Task.Delay(100);
        }
        profiles.Should().ContainSingle("o perfil ActivoBank é criado no signup");

        await using var conn = _fixture.OpenAppConnection();
        await using (var set = new NpgsqlCommand(
            "SELECT set_config('app.current_tenant_id', @t, false)", conn))
        {
            set.Parameters.AddWithValue("t", tenantA.ToString());
            await set.ExecuteScalarAsync();
        }
        await using var read = new NpgsqlCommand(
            "SELECT count(*) FROM financial.import_profiles WHERE tenant_id = @b", conn);
        read.Parameters.AddWithValue("b", tenantB);
        ((long)(await read.ExecuteScalarAsync())!).Should().Be(0);
    }

    [Fact]
    public async Task Cross_tenant_insert_into_categorization_rules_is_rejected()
    {
        var (_, tenantA, _) = await FinancialTestHelpers.SignupAndLoginAsync(_fixture, "rls-rule-a");
        var (_, tenantB, _) = await FinancialTestHelpers.SignupAndLoginAsync(_fixture, "rls-rule-b");

        await using var conn = _fixture.OpenAppConnection();
        await using (var set = new NpgsqlCommand(
            "SELECT set_config('app.current_tenant_id', @t, false)", conn))
        {
            set.Parameters.AddWithValue("t", tenantA.ToString());
            await set.ExecuteScalarAsync();
        }

        await using var insert = new NpgsqlCommand(
            "INSERT INTO financial.categorization_rules " +
            "(id, tenant_id, name, pattern, match_type, category_id, priority, is_active, created_at, updated_at, version) " +
            "VALUES (@id, @b, 'x', 'x', 'Contains', @c, 999, true, now(), now(), 1)", conn);
        insert.Parameters.AddWithValue("id", Guid.NewGuid());
        insert.Parameters.AddWithValue("b", tenantB);
        insert.Parameters.AddWithValue("c", Guid.NewGuid());

        var act = async () => await insert.ExecuteNonQueryAsync();
        (await act.Should().ThrowAsync<PostgresException>()).Which.SqlState.Should().Be("42501");
    }
}
```

Antes de correr, confirmar os nomes de colunas de `categorization_rules` em `20260430053400_AddCsvImport.cs` e ajustar o INSERT se diferirem. Acrescentar `using System.Net.Http.Json;`.

- [ ] **Step 2: Correr e ver falhar**

Run: `dotnet test tests/Sextante.IntegrationTests --filter "FullyQualifiedName~ImportTablesRlsTests"`
Expected: FAIL em `Table_has_rls_enabled_and_forced` (3×) e no insert cross-tenant (não lança).

- [ ] **Step 3: Migration**

Gerar vazia: `dotnet ef migrations add AddMissingRlsToImportTables --project src/Modules/Financial/Sextante.Modules.Financial.Infrastructure --startup-project src/Bootstrap/Sextante.Host --context FinancialDbContext` (confirmar que o snapshot não muda; se o EF gerar operações, removê-las). Corpo:

```csharp
protected override void Up(MigrationBuilder migrationBuilder)
{
    // Phase 6.5 §0.2 — a migration AddCsvImport (Phase 4) criou estas
    // três tabelas sem RLS. Mesmo padrão de AddBudgetsAndAlerts.
    foreach (var table in Tables)
    {
        migrationBuilder.Sql($"""
            ALTER TABLE financial.{table} ENABLE ROW LEVEL SECURITY;
            ALTER TABLE financial.{table} FORCE ROW LEVEL SECURITY;
            CREATE POLICY {table}_tenant_isolation ON financial.{table}
                USING (tenant_id = current_setting('app.current_tenant_id', false)::uuid)
                WITH CHECK (tenant_id = current_setting('app.current_tenant_id', false)::uuid);
            ALTER TABLE financial.{table}
                ADD CONSTRAINT {table}_tenant_id_not_sentinel
                CHECK (tenant_id <> 'ffffffff-ffff-ffff-ffff-ffffffffffff'::uuid);
            DO $$
            BEGIN
                IF EXISTS (SELECT FROM pg_roles WHERE rolname = 'sextante_app') THEN
                    GRANT SELECT, INSERT, UPDATE, DELETE ON financial.{table} TO sextante_app;
                END IF;
            END
            $$;
            """);
    }
}

protected override void Down(MigrationBuilder migrationBuilder)
{
    foreach (var table in Tables)
    {
        migrationBuilder.Sql($"""
            DROP POLICY IF EXISTS {table}_tenant_isolation ON financial.{table};
            ALTER TABLE financial.{table} DISABLE ROW LEVEL SECURITY;
            ALTER TABLE financial.{table} DROP CONSTRAINT IF EXISTS {table}_tenant_id_not_sentinel;
            """);
    }
}

private static readonly string[] Tables =
    ["categorization_rules", "import_profiles", "import_batches"];
```

- [ ] **Step 4: Seed do perfil com tenant**

`IImportProfileRepository`:

```csharp
/// <summary>
/// Grava o perfil fora de uma request HTTP (subscriber do signup),
/// definindo <c>app.current_tenant_id</c> para a duração da transação.
/// </summary>
Task SeedAsync(Guid tenantId, ImportProfile profile, CancellationToken cancellationToken);
```

`ImportProfileRepository` (mesmo padrão de `CategoryRepository.SeedAsync`):

```csharp
public async Task SeedAsync(Guid tenantId, ImportProfile profile, CancellationToken cancellationToken)
{
    await using var transaction = await _db.Database.BeginTransactionAsync(cancellationToken);

    // set_config(.., true) limita o scope ao TX. RLS WITH CHECK aceita.
    await _db.Database.ExecuteSqlRawAsync(
        "SELECT set_config('app.current_tenant_id', {0}, true)",
        new object[] { tenantId.ToString() },
        cancellationToken);

    await _db.ImportProfiles.AddAsync(profile, cancellationToken);
    await _db.SaveChangesAsync(cancellationToken);
    await transaction.CommitAsync(cancellationToken);
}
```

`SeedDefaultImportProfilesHandler`: trocar `AddAsync` + `SaveChangesAsync` por `await repository.SeedAsync(@event.TenantId, profile, cancellationToken);`. Atualizar stubs de `IImportProfileRepository` nos testes unitários, se existirem (`grep -rn "IImportProfileRepository" tests`).

- [ ] **Step 5: Correr os testes**

Run: `dotnet test tests/Sextante.IntegrationTests --filter "FullyQualifiedName~ImportTablesRlsTests|FullyQualifiedName~CsvImport|FullyQualifiedName~CategorizationRule|FullyQualifiedName~ImportProfile"`
Expected: PASS.

- [ ] **Step 6: Commit**

```bash
git add -A src/Modules/Financial tests/Sextante.IntegrationTests/MultiTenancy/ImportTablesRlsTests.cs
git commit -m "Ativar RLS nas tabelas do import e semear perfil com tenant"
```

---

### Task 3: Reaplicar regras sem limite e com eventos

**Files:**
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Application/Features/CategorizationRules/CategorizationRuleHandlers.cs:149-203`
- Test: `tests/Modules/Financial.Application.Tests/CategorizationRules/ReapplyCategorizationRulesHandlerTests.cs`

**Interfaces:**
- Consumes: `ITransactionRepository.ListAsync` (cursor `TransactionCursor`, página máx. 100), `IIntegrationEventPublisher`.
- Produces: handler com assinatura `Handle(ReapplyCategorizationRulesCommand, ITransactionRepository, ICategorizationRuleEngine, ITenantContext, IIntegrationEventPublisher, CancellationToken)`.

- [ ] **Step 1: Testes que falham**

No `StubTxRepo` do ficheiro de testes, respeitar `PageSize` e `Cursor` (ordem `OccurredAt` desc, `Id` desc), igual ao repositório real:

```csharp
public Task<TransactionPage> ListAsync(TransactionFilter filter, CancellationToken ct)
{
    LastFilter = filter;
    IEnumerable<Transaction> filtered = Items;
    if (filter.DateFrom.HasValue) filtered = filtered.Where(t => t.OccurredAt >= filter.DateFrom.Value);
    if (filter.DateTo.HasValue) filtered = filtered.Where(t => t.OccurredAt <= filter.DateTo.Value);
    if (filter.CategoryIds is { Count: > 0 }) filtered = filtered.Where(t => filter.CategoryIds.Contains(t.CategoryId));
    var ordered = filtered.OrderByDescending(t => t.OccurredAt).ThenByDescending(t => t.Id).ToList();
    if (filter.Cursor is { } c)
        ordered = ordered.Where(t => t.OccurredAt < c.OccurredAt
            || (t.OccurredAt == c.OccurredAt && t.Id.CompareTo(c.Id) < 0)).ToList();
    var size = Math.Clamp(filter.PageSize, 1, 100);
    var page = ordered.Take(size).ToList();
    var next = ordered.Count > size ? new TransactionCursor(page[^1].OccurredAt, page[^1].Id) : null;
    return Task.FromResult(new TransactionPage(page, next));
}
```

Novos testes (e passar `new StubIntegrationEventPublisher()` em todas as chamadas existentes a `Handle`):

```csharp
[Fact]
public async Task Processes_every_page_beyond_the_100_row_limit()
{
    var account = Guid.NewGuid();
    var oldCategory = Guid.NewGuid();
    var newCategory = Guid.NewGuid();
    var baseDate = DateTimeOffset.UtcNow.AddDays(-400);
    var items = Enumerable.Range(0, 250)
        .Select(i => NewTx(account, oldCategory, baseDate.AddDays(i), 10m, "Continente"))
        .ToArray();
    var txRepo = new StubTxRepo(items);
    var engine = new StubEngine(matchToCategory: newCategory, matchedRule: Guid.NewGuid());
    var events = new StubIntegrationEventPublisher();

    var response = await CategorizationRuleHandlers.Handle(
        new ReapplyCategorizationRulesCommand(oldCategory, null, null, OnlyUncategorized: false),
        txRepo, engine, new StubTenantContext(Tenant), events, CancellationToken.None);

    response.TotalProcessed.Should().Be(250);
    response.CategorizedCount.Should().Be(250);
    items.Should().OnlyContain(t => t.CategoryId == newCategory);
}

[Fact]
public async Task Publishes_updated_event_for_each_recategorized_transaction()
{
    var newCategory = Guid.NewGuid();
    var t1 = NewTx(Guid.NewGuid(), Guid.NewGuid(), DateTimeOffset.UtcNow.AddDays(-1), 10m, "Continente");
    var t2 = NewTx(Guid.NewGuid(), newCategory, DateTimeOffset.UtcNow.AddDays(-1), 20m, "Continente");
    var events = new StubIntegrationEventPublisher();

    await CategorizationRuleHandlers.Handle(
        new ReapplyCategorizationRulesCommand(null, null, null, OnlyUncategorized: false),
        new StubTxRepo(t1, t2),
        new StubEngine(matchToCategory: newCategory, matchedRule: Guid.NewGuid()),
        new StubTenantContext(Tenant), events, CancellationToken.None);

    events.Published.OfType<TransactionUpdatedIntegrationEvent>()
        .Select(e => e.TransactionId).Should().BeEquivalentTo(new[] { t1.Id },
            "t2 já tinha a categoria — não mudou, não publica");
}
```

Adicionar `using Sextante.Modules.Financial.Application.Tests.TestSupport;` e `using Sextante.Modules.Financial.PublicApi.Events;` (confirmar referência do projeto de testes ao `PublicApi`).

- [ ] **Step 2: Ver falhar**

Run: `dotnet test tests/Modules/Financial.Application.Tests --filter "FullyQualifiedName~ReapplyCategorizationRules"`
Expected: FAIL de compilação (argumento `events`), depois FAIL em `TotalProcessed` 100 ≠ 250.

- [ ] **Step 3: Implementação**

```csharp
public static async Task<ReapplyCategorizationRulesResponse> Handle(
    ReapplyCategorizationRulesCommand command,
    ITransactionRepository txRepo,
    ICategorizationRuleEngine engine,
    ITenantContext tenant,
    IIntegrationEventPublisher events,
    CancellationToken ct)
{
    // Phase 6.5 §0.3 — o repositório limita cada página a 100 linhas;
    // percorrer por cursor até ao fim em vez de pedir PageSize=10000.
    var categoryIds = command.CategoryId is not null ? new[] { command.CategoryId.Value } : null;
    var allTransactions = new List<Transaction>();
    TransactionCursor? cursor = null;
    do
    {
        var page = await txRepo.ListAsync(
            new TransactionFilter(command.From, command.To, categoryIds, null, null, ReapplyPageSize, cursor),
            ct);
        allTransactions.AddRange(page.Items);
        cursor = page.NextCursor;
    }
    while (cursor is not null);

    if (command.OnlyUncategorized)
    {
        allTransactions = allTransactions.Where(t => t.CategorizationRuleId is null).ToList();
    }

    var toProcess = allTransactions
        .Where(t => !string.IsNullOrWhiteSpace(t.Description))
        .Select(t => new TransactionToCategorize(t.Id, t.Description!))
        .ToList();

    var results = await engine.ApplyAsync(toProcess, tenant.TenantId, ct);

    var changed = new List<Transaction>();
    foreach (var result in results)
    {
        if (result.NewCategoryId is null) continue;

        var tx = allTransactions.FirstOrDefault(t => t.Id == result.TransactionId);
        if (tx is null) continue;

        if (tx.CategoryId == result.NewCategoryId.Value) continue;

        tx.SetCategory(result.NewCategoryId.Value);
        if (result.MatchedRuleId is not null)
            tx.MarkCategorizedByRule(result.MatchedRuleId.Value);

        txRepo.Update(tx);
        changed.Add(tx);
    }

    await txRepo.SaveChangesAsync(ct);

    // Orçamentos recalculam a partir destes eventos (Phase 5b).
    foreach (var tx in changed)
    {
        await events.PublishAsync(
            new TransactionUpdatedIntegrationEvent(
                tx.Id,
                tenant.TenantId.Value,
                tx.AccountId,
                tx.CategoryId,
                tx.Amount.Amount,
                tx.Amount.Currency,
                tx.OccurredAt,
                DateTimeOffset.UtcNow),
            ct);
    }

    return new ReapplyCategorizationRulesResponse(
        toProcess.Count,
        changed.Count,
        toProcess.Count - changed.Count);
}

private const int ReapplyPageSize = 100;
```

Acrescentar `using Sextante.Modules.Financial.Application.Common;` e `using Sextante.Modules.Financial.PublicApi.Events;` se faltarem.

- [ ] **Step 4: Testes passam**

Run: `dotnet test tests/Modules/Financial.Application.Tests --filter "FullyQualifiedName~ReapplyCategorizationRules"`
Expected: PASS (7 testes).

- [ ] **Step 5: Commit**

```bash
git add src/Modules/Financial/Sextante.Modules.Financial.Application/Features/CategorizationRules/CategorizationRuleHandlers.cs tests/Modules/Financial.Application.Tests/CategorizationRules/ReapplyCategorizationRulesHandlerTests.cs
git commit -m "Reaplicar regras em todas as transações e publicar eventos"
```

---

### Task 4: Totais incluem transações de categorias arquivadas

**Files:**
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Infrastructure/Persistence/Repositories/TransactionRepository.cs:64-244`
- Test: `tests/Sextante.IntegrationTests/Financial/ArchivedCategoryTotalsTests.cs`

**Interfaces:** Consumes: `FinancialDbContext.Categories`. Produces: nenhum contrato novo.

- [ ] **Step 1: Teste que falha**

Fluxo: criar conta, categoria de despesa "Temporária", transação de 40 €, obter `summary` do mês e `by-category`, arquivar a transação? **Não** — a regra de domínio impede arquivar categoria com transações ativas. O caso real é: transação criada, depois recategorizada? Não. Caminho que produz a situação: arquivar a categoria diretamente na DB (superuser `UPDATE financial.categories SET deleted_at = now()`), simulando dados legados/import. Asserts: `summary.expense` continua 40; `by-category` contém a categoria com 40; export contém a linha com o nome da categoria.

```csharp
[Fact]
public async Task Summary_by_category_and_export_keep_transactions_of_archived_categories()
{
    var (client, _, _) = await FinancialTestHelpers.SignupAndLoginAsync(_fixture, "arch-cat");
    var accountId = await CreateAccountAsync(client);
    var categoryId = await CreateCategoryAsync(client, "Temporária", kind: 0);
    await CreateTransactionAsync(client, accountId, categoryId, 40m, DateTimeOffset.UtcNow.AddDays(-1));

    await using (var super = _fixture.OpenSuperuserConnection())
    await using (var cmd = new NpgsqlCommand(
        "UPDATE financial.categories SET deleted_at = now() WHERE id = @id", super))
    {
        cmd.Parameters.AddWithValue("id", categoryId);
        await cmd.ExecuteNonQueryAsync();
    }

    var from = DateTimeOffset.UtcNow.AddDays(-7).ToString("O");
    var to = DateTimeOffset.UtcNow.ToString("O");

    var summary = await client.GetFromJsonAsync<JsonElement>(
        $"/api/financial/transactions/summary?from={Uri.EscapeDataString(from)}&to={Uri.EscapeDataString(to)}");
    summary.GetProperty("expense").GetProperty("amount").GetDecimal().Should().Be(40m);

    var byCategory = await client.GetFromJsonAsync<List<JsonElement>>(
        $"/api/financial/transactions/by-category?kind=Expense&from={Uri.EscapeDataString(from)}&to={Uri.EscapeDataString(to)}");
    byCategory.Should().Contain(r => r.GetProperty("categoryName").GetString() == "Temporária");

    var csv = await client.GetStringAsync(
        $"/api/financial/transactions/export?from={Uri.EscapeDataString(from)}&to={Uri.EscapeDataString(to)}");
    csv.Should().Contain("Temporária");
}
```

Antes de escrever os helpers `CreateAccountAsync`/`CreateCategoryAsync`/`CreateTransactionAsync`, copiar os de `BudgetWorkflowTests.cs` (mesmo payload) e confirmar nomes dos query params e das propriedades JSON em `TransactionsEndpoints.cs:49-120` e em `TransactionsContracts.cs` (ajustar `from`/`to`/`kind`/`categoryName`/`expense.amount` ao contrato real).

- [ ] **Step 2: Ver falhar**

Run: `dotnet test tests/Sextante.IntegrationTests --filter "FullyQualifiedName~ArchivedCategoryTotalsTests"`
Expected: FAIL — expense 0.

- [ ] **Step 3: Implementação**

No `TransactionRepository`, uma propriedade privada que ignora os filtros globais das categorias mas junta por tenant (a transação já vem filtrada por tenant, e a RLS continua ativa):

```csharp
// Phase 6.5 §0.4 — categorias arquivadas (soft-delete) continuam a
// nomear as transações antigas. IgnoreQueryFilters tira também o filtro
// de tenant, por isso o join é sempre por (Id, TenantId); a RLS da DB
// é a segunda barreira.
private IQueryable<Category> CategoriesIncludingArchived
    => _db.Categories.IgnoreQueryFilters();
```

Em `GetConvertedTotalsAsync`, `GetTotalsByCurrencyAsync`, `ListForExportAsync` e `GetByCategoryAsync`, trocar cada `.Join(_db.Categories…, t => t.CategoryId, c => c.Id, …)` por um join composto:

```csharp
.Join(
    CategoriesIncludingArchived,
    t => new { Id = t.CategoryId, t.TenantId },
    c => new { c.Id, c.TenantId },
    (t, c) => …)
```

(no export o lado esquerdo é `x.Transaction`; no `GetByCategoryAsync` aplicar `.Where(c => c.Kind == kind)` sobre `CategoriesIncludingArchived`). No `ApplyFilter` (:236-244):

```csharp
query = query.Where(t =>
    CategoriesIncludingArchived.Any(c =>
        c.Id == t.CategoryId && c.TenantId == t.TenantId && c.Kind == kind));
```

Confirmar que `TenantId` é mapeado igual nos dois lados (value converter de `TenantId`); se o EF não traduzir o tipo anónimo com `TenantId`, comparar `.Value`.

- [ ] **Step 4: Testes passam**

Run: `dotnet test tests/Sextante.IntegrationTests --filter "FullyQualifiedName~ArchivedCategoryTotalsTests|FullyQualifiedName~TransactionFiltering|FullyQualifiedName~TransactionExport|FullyQualifiedName~FinancialMultiTenancy"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/Modules/Financial/Sextante.Modules.Financial.Infrastructure/Persistence/Repositories/TransactionRepository.cs tests/Sextante.IntegrationTests/Financial/ArchivedCategoryTotalsTests.cs
git commit -m "Manter transações de categorias arquivadas nos totais e no export"
```

---

### Task 5: Import publica eventos e usa a moeda primária do tenant

**Files:**
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Application/Features/CsvImport/CsvImportHandlers.cs:249-443`
- Test: `tests/Sextante.IntegrationTests/Financial/CsvImportEventsTests.cs`

**Interfaces:**
- Consumes: `ITenantCurrencyResolver.GetPrimaryCurrencyAsync(CancellationToken)` (já usado em `TransactionHandlers.cs:33`), `IIntegrationEventPublisher`.
- Produces: confirm handler com dois parâmetros novos (`ITenantCurrencyResolver currencyResolver`, `IIntegrationEventPublisher events`).

- [ ] **Step 1: Teste que falha**

Teste de integração: signup; conta; categoria de despesa com orçamento de 100 € no mês corrente (payload de `BudgetWorkflowTests.CreateBudgetAsync`); regra `Contains "CONTINENTE"` → essa categoria; upload de CSV sintético com uma linha `CONTINENTE` de −90,00 datada de hoje; confirm; depois invocar o handler de alertas como em `BudgetWorkflowTests.DispatchForCategoryAsync` **só se** o evento tiver sido publicado. Para tornar o assert determinístico sem esperar pelo outbox, substituir o `IIntegrationEventPublisher` no `WebApplicationFactory` por um recorder? Mais simples e alinhado com o existente: **teste unitário** do handler com stubs, em `tests/Modules/Financial.Application.Tests/CsvImport/ConfirmImportPublishesEventsTests.cs`, verificando `StubIntegrationEventPublisher.Published` contém um `TransactionCreatedIntegrationEvent` por linha importada e que a moeda primária usada vem do resolver (conta em USD + primária EUR → `ResolveAsync("USD","EUR",…)` chamado; conta em EUR + primária BRL → `ResolveAsync("EUR","BRL",…)`).

Ler primeiro os stubs já existentes em `tests/Modules/Financial.Application.Tests/CsvImport/` para reutilizar `IImportBatchRepository`/`IImportProfileRepository`/`IAccountRepository`/`ICategoryRepository` fakes; criar os que faltarem no mesmo ficheiro de teste.

- [ ] **Step 2: Ver falhar**

Run: `dotnet test tests/Modules/Financial.Application.Tests --filter "FullyQualifiedName~ConfirmImportPublishesEvents"`
Expected: FAIL de compilação, depois FAIL (nenhum evento; moeda "EUR").

- [ ] **Step 3: Implementação**

- Adicionar parâmetros `ITenantCurrencyResolver currencyResolver` e `IIntegrationEventPublisher events` ao `Handle(ConfirmImportCommand …)`.
- Antes do `foreach`: `var primaryCurrency = await currencyResolver.GetPrimaryCurrencyAsync(ct);` e `var created = new List<Transaction>();`.
- Remover `var primaryCurrency = "EUR";` (:349).
- Depois de `await txRepo.AddAsync(tx, ct);` → `created.Add(tx);`.
- Depois de `await batchRepo.SaveChangesAsync(ct);`:

```csharp
// Phase 6.5 §0.5 — orçamentos e alertas (Phase 5b) só reagem a eventos;
// o import tem de os publicar como qualquer outra criação.
foreach (var tx in created)
{
    await events.PublishAsync(
        new TransactionCreatedIntegrationEvent(
            tx.Id,
            tenant.TenantId.Value,
            tx.AccountId,
            tx.CategoryId,
            tx.Amount.Amount,
            tx.Amount.Currency,
            tx.OccurredAt,
            DateTimeOffset.UtcNow),
        ct);
}
```

- [ ] **Step 4: Testes passam**

Run: `dotnet test tests/Modules/Financial.Application.Tests --filter "FullyQualifiedName~CsvImport"` e `dotnet test tests/Sextante.IntegrationTests --filter "FullyQualifiedName~CsvImport"`
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add src/Modules/Financial/Sextante.Modules.Financial.Application/Features/CsvImport/CsvImportHandlers.cs tests/Modules/Financial.Application.Tests/CsvImport/ConfirmImportPublishesEventsTests.cs
git commit -m "Import publica eventos de criação e usa a moeda primária do tenant"
```

---

### Task 6: Arquivar transação publica evento; arquivar conta com transações é recusado

**Files:**
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Application/Features/Transactions/TransactionHandlers.cs:125-140`
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Application/Features/Accounts/AccountHandlers.cs:62-77`
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Domain/Accounts/Account.cs` (`EnsureCanArchive`)
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Domain/Common/DomainException.cs` (`AccountHasActiveTransactionsException`)
- Modify: `src/Modules/Financial/Sextante.Modules.Financial.Api/Endpoints/AccountsEndpoints.cs:55-59`
- Test: `tests/Modules/Financial.Domain.Tests/Accounts/` (novo teste), `tests/Sextante.IntegrationTests/Financial/AccountsCrudTests.cs`

**Interfaces:**
- Produces: `Account.EnsureCanArchive(int activeTransactionCount)`; `AccountHasActiveTransactionsException(int activeCount)` com `ActiveCount`.

- [ ] **Step 1: Testes que falham**

Domínio:

```csharp
[Fact]
public void EnsureCanArchive_throws_when_account_has_active_transactions()
{
    var account = Account.Create("Conta", AccountType.Checking, "EUR", new Money(0m, "EUR"), TenantId.New());
    var act = () => account.EnsureCanArchive(3);
    act.Should().Throw<AccountHasActiveTransactionsException>().Which.ActiveCount.Should().Be(3);
}

[Fact]
public void EnsureCanArchive_passes_without_transactions()
{
    var account = Account.Create("Conta", AccountType.Checking, "EUR", new Money(0m, "EUR"), TenantId.New());
    account.Invoking(a => a.EnsureCanArchive(0)).Should().NotThrow();
}
```

Integração (em `AccountsCrudTests`): conta com uma transação → `DELETE /api/financial/accounts/{id}` devolve 400 e `GET` da conta devolve 200; conta sem transações → 204.

- [ ] **Step 2: Ver falhar**

Run: `dotnet test tests/Modules/Financial.Domain.Tests --filter "FullyQualifiedName~EnsureCanArchive"` e `dotnet test tests/Sextante.IntegrationTests --filter "FullyQualifiedName~AccountsCrudTests"`
Expected: FAIL.

- [ ] **Step 3: Implementação**

`DomainException.cs`:

```csharp
public sealed class AccountHasActiveTransactionsException : FinancialDomainException
{
    public AccountHasActiveTransactionsException(int activeCount)
        : base($"Não é possível arquivar uma conta com {activeCount} transações ativas.")
    {
        ActiveCount = activeCount;
    }

    public int ActiveCount { get; }
}
```

`Account.cs`:

```csharp
/// <summary>
/// Conta com transações ativas não arquiva — o saldo e o histórico
/// deixariam de ser explicáveis.
/// </summary>
/// <exception cref="AccountHasActiveTransactionsException"/>
public void EnsureCanArchive(int activeTransactionCount)
{
    if (activeTransactionCount > 0)
    {
        throw new AccountHasActiveTransactionsException(activeTransactionCount);
    }
}
```

`AccountHandlers.cs` (archive): depois de obter a conta, `var activeCount = await repository.CountActiveTransactionsAsync(account.Id, cancellationToken); account.EnsureCanArchive(activeCount);`.

`AccountsEndpoints.cs` (DELETE), mesmo padrão de `CategoriesEndpoints.cs:54-64`:

```csharp
group.MapDelete("{id:guid}", async (Guid id, IMessageBus bus, CancellationToken ct) =>
{
    try
    {
        var archived = await bus.InvokeAsync<bool>(new ArchiveAccountCommand(id), ct);
        return archived ? Results.NoContent() : Results.NotFound();
    }
    catch (AccountHasActiveTransactionsException ex)
    {
        return Results.ValidationProblem(
            new Dictionary<string, string[]>
            {
                ["account"] = ["Não é possível arquivar uma conta com transações ativas."],
            },
            detail: ex.Message,
            title: "Erros de validação",
            statusCode: StatusCodes.Status400BadRequest);
    }
});
```

`TransactionHandlers.cs` (archive): acrescentar `ITenantContext tenant, IIntegrationEventPublisher events` e, depois do `SaveChangesAsync`, publicar `TransactionUpdatedIntegrationEvent` com os dados da transação (o `BudgetProgressService` já exclui apagadas, por isso o recálculo baixa o consumo).

- [ ] **Step 4: Testes passam**

Run: os dois comandos do Step 2 + `dotnet test tests/Sextante.IntegrationTests --filter "FullyQualifiedName~TransactionsCrudTests"`.
Expected: PASS.

- [ ] **Step 5: Commit**

```bash
git add -A src/Modules/Financial tests
git commit -m "Recusar arquivar conta com transações e publicar evento ao arquivar transação"
```

---

### Task 7: Verificação do grupo

- [ ] `dotnet build Sextante.slnx` sem warnings novos.
- [ ] `dotnet format Sextante.slnx --verify-no-changes`.
- [ ] `dotnet test Sextante.slnx` — tudo verde (comparar com a baseline anterior ao grupo).
- [ ] Atualizar a stack local: `docker compose up -d --build`; confirmar no Postgres local que as três tabelas têm `relrowsecurity = t` e que o signup cria o perfil de importação.
- [ ] Marcar o primeiro item da Phase 6.5 no `roadmap.md` como parcialmente feito (ADR + dívida técnica) só no fim da phase — não neste grupo.
